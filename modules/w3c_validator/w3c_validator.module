<?php

use Drupal\user\Entity\User;

function w3c_validator_cron() {
  $context = array();

  // Do validation as admin user.
  // $user = User::load(1);

  // \Drupal::service('w3c.processor')->validateAllPages($context, 10, $user);
}